<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project1_galor_siboni">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SqlProfileView"></activity>
        <activity android:name=".SqlProfileEdit" />
        <activity android:name=".MongoProfileView" />
        <activity android:name=".MongoProfileEdit" />
        <activity android:name=".SqlLogin" />
        <activity android:name=".MongoLogin" />
        <activity android:name=".FirebaseMain" />
        <activity android:name=".ProfileView" />
        <activity android:name=".ProfileEdit" />
        <activity android:name=".platform_chooser">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>